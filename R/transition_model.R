transition_model <- function(patient, event, ctx) {
  # TODO:
  # Branch on event$event_type and return a named list of updates or NULL.
  #
  # For panel updates:
  #   upd <- patientSimCore::update_block(patient, "panel", list(x=..., y=...))
  # Combine with other updates:
  #   patientSimCore::combine_updates(list(a=1), upd)
  NULL
}
