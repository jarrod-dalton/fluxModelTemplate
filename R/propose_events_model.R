propose_events_model <- function(patient, ctx) {
  # TODO:
  # Return a named list of event candidates across your processes.
  # Each candidate should include: time_next, event_type, process_id
  #
  # Return NULL for a process if nothing is pending.
  list()
}
